%h2.hero Learn programming by making practice quizzes and taking them until you can get 100%.  Master topics by acing practice quizzes created by your peers.
.button
  - if user_signed_in?
    %a.btn.btn-large.btn-primary{:href => "quizzes/new"} Make a new quiz
  - else
    %a.btn.btn-large.btn-primary{:href => "users/sign_up"} Log in to make your own quiz

%table
  - @quizzes.each do |quiz|
    %tr
      %td
        %h3= link_to quiz.body, quiz
      - if can? :destroy, quiz
        %td= link_to "Destroy", quiz, method: :delete, :confirm => "Are you sure you want to delete this quiz?"
