= form_for [@quiz, @question] do |f|
  %p= f.label :body
  %p= f.text_area :body, :rows => "15", :class => "form-control"
  %p= f.label :answer
  %p= f.text_area :answer, :rows => "15", :class => "form-control"
  %p= f.submit
